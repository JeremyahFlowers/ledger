{
  "name": "ledger",
  "version": "1.0.0",
  "description": "A standalone interview-prep system: spaced repetition, pattern recognition, and a well-being model that treats burnout as a first-class failure.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test 'test/*.test.js'",
    "test:coverage": "node --test --experimental-test-coverage 'test/*.test.js'",
    "test:watch": "node --test --watch 'test/*.test.js'",
    "check": "node scripts/check-modules.mjs && node scripts/check-references.mjs && node scripts/check-views.mjs && node scripts/check-styles.mjs",
    "verify": "npm run check && npm test",
    "dev": "python3 scripts/dev-server.py",
    "measure": "node scripts/measure.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
